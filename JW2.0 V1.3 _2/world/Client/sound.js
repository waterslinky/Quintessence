
// music=new Audio("Client/music/music.mp3")
// music.volume=0.2

// ascend_song=new Audio("Client/music/ascend_song.mp3")
// ascend_song.volume=0.7

music=new Audio("Client/music/Over_World_Song.mp4") 
music.volume=0.05

// music.loop()
// window.addEventListener("load", event => {

//     music.play()
// })


// function startefewrrf(){
//      AudioContext = window.AudioContext || window.webkitAudioContext;

//  audioContext = new AudioContext();

// // get the audio element
//  audioElement= new Audio("music.mp3")

// // pass it into the audio context
//  track = audioContext.createMediaElementSource(audioElement);

//  stereoNode = new StereoPannerNode(audioContext, { pan: 0 });

// // change the value of the balance by updating the pan value

// stereoNode.pan.value = -1; // right

// track.connect(stereoNode).connect(audioContext.destination);
// audioElement.volume=1
//     audioElement.play()


// }
